{
  "name": "nuxt_in_docker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate": "BASE=/nuxt_practice/ nuxt build && nuxt export",
    "dev": "nuxt",
    "test": "jest",
    "build": "nuxt build",
    "start": "nuxt start",
    "export": "nuxt export",
    "serve": "nuxt serve"
  },
  "dependencies": {
    "node-sass": "^4.14.1",
    "nuxt": "^2.13.0",
    "sass-loader": "^9.0.2"
  },
  "devDependencies": {
    "@vue/test-utils": "^1.0.3",
    "babel-core": "7.0.0-bridge.0",
    "babel-jest": "^26.0.1",
    "jest": "^26.0.1",
    "vue-jest": "^3.0.4"
  }
}